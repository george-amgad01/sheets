#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {

}
void right_max (ll arr [] , ll ind ) {
    if (ind == 0  ) {
        return  ;
    }
    arr[ind-1] = max(arr[ind-1], arr[ind]);
    return right_max(arr , ind-1 );
}



int main() {
    George;
    int t =1 ;
    //cin >> t;
    ll n ; cin >> n;
    ll arr [n];
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
    }
    ll ind = n -1 ;
    while (t--) {
        right_max(arr , ind );
        cout << nl ;
        for (ll i = 0 ; i < n ; i++) {
            cout << arr[i] << " ";
        }

        solve();
    }
}
