#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}
void left_max (ll arr [] , ll n ) {
    if ( n == 1) {
        return  ;
    }
    left_max(arr , n-1 );
    arr[n-1] = max(arr[n-1], arr[n-2]);
}



int main() {
    George;
    int t =1 ;
    //cin >> t;
    ll n ; cin >> n;
    ll arr [n];
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
    }
    while (t--) {
        left_max(arr , n );
        for (ll i = 0 ; i < n ; i++) {
            cout << arr[i] << " ";
        }

        solve();
    }
}
