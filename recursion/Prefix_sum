#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {

}
ll prefix_sum (vector<ll>& v , ll n , ll goal) {
    if (goal == 0  ) { // 1 2
        return 0;
    }

    return v[goal-1] + prefix_sum(v, n-1, goal-1);
}


int main() {
    George;
    int t =1 ;
    //cin >> t;
    ll n , goal ; cin >> n >> goal ;
    vector <ll> v(n);
    for (ll i = 0 ; i < n ; i++) {
        cin >> v[i];
    }
    while (t--) {
        cout<< nl ;
        cout << prefix_sum (v , n, goal);
        solve();
    }
}
