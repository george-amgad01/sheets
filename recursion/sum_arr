#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}

ll sum_arr (ll arr[] , ll n ) {
    if (n == 1 ) {
        return 1 ;
    }
    return arr[n-1] + sum_arr(arr, n - 1);
}


int main() {
    George;
    int t = 1;
    ll n ; cin >> n;
    ll arr[n] ;
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
    }
    //cin >> t;
    while (t--) {
        cout << sum_arr(arr, n) ;
        solve();
    }
}
