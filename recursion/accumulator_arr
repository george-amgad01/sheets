#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}

void accumulator_arr (ll arr [] , ll n) {
    if (n ==1 ) {
        return;
    }

    accumulator_arr (arr , n-1);
    arr[n-1] += arr[n-2] ; // اول عنصر بيرجع 2   arr[1] += arr[0]
}


int main() {
    George;
    int t = 1;
    ll n ; cin >> n;
    ll arr[n] ;
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
    }
    //cin >> t;
    while (t--) {
        accumulator_arr (arr , n);
        for (ll i = 0 ; i < n ; i++) {
            cout << arr[i] << " " ;
        }
        solve();
    }
}
