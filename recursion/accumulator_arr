#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}

void accumulator_arr (ll arr [] , ll n) {
    static ll acc = arr[0] ;
    if (n ==1 ) {
        return;
    }

    accumulator_arr (arr , n-1);
    arr[n-1] += acc ;
    acc += arr[n-1] ;
}


int main() {
    George;
    int t = 1;
    ll n ; cin >> n;
    ll arr[n] ;
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
    }
    //cin >> t;
    while (t--) {
        accumulator_arr (arr , n);
        for (ll i = 0 ; i < n ; i++) {
            cout << arr[i] << " " ;
        }
        solve();
    }
}
