#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {

}
ll suffix_sum ( vector <ll>& v , ll n , ll goal ) {
    if (goal == 0 ) {
        return 0;
    }
    return v[n-1] + suffix_sum (v , n-1, goal-1);
}



int main() {
    George;
    int t =1 ;
    //cin >> t;
    ll res = 0 ;
    ll n , goal ; cin >> n >> goal ;
    vector <ll> v(n);
    for (ll i = 0 ; i < n ; i++) {
        cin >> v[i];
    }
    while (t--) {
        cout << suffix_sum (v , n, goal );
        solve();
    }
}
